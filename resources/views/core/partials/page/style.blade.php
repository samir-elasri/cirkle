<style>

	:root {
		/* Défini la largeur optimale du contenu */
		--optimal-content-width: {{ $optimal_content_width }}px;

		/* Défini l'espacement par défaut entre les blocs */
		--default-bloc-spacing: {{ $default_bloc_spacing }}px;

		/* Défini l'espacement dans les blocs ayant une couleur de fond ou une image */
		--default-bloc-inner-spacing: {{ $default_bloc_inner_spacing }}px;

		/* Défini l'espacement entre le contenu et la colonne de droite */
		--right-column-spacing: {{ $right_column_spacing }}px;

		/* Défini la largeur de la colonne de droite */
		--right-column-width: {{ $right_column_width }}px;

		/* Défini la hauteur hauteur des image de mini-fiches */
		--single-header-image-height: {{ $default_single_image_height }}px;
	}

</style>
